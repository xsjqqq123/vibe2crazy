todo:

如果文件大小超过500KB，则提示文件过大。由服务端实现。编辑、diff比较与commits预览等涉及文件传输的均需要此功能。

在Accept Changes(commit)时，要求用户输入变更内容，commits列表中，标题应显示提交git的消息内容。

project在创建task时，可勾选“Directly on the branch”，这个task直接在project创建时的所选的分支代码上进行更改，而不是创建worktree。
代码commit时直接提交到这个分支，但它是不支持merge的。

git在空目录上创建项目后，从worktree中调用merge时会报错。

在code review中执行merge时会调用git fetch origin xx，而我只希望拉取xx分支（worktree所对应分支最新提交）

/ralph-loop:ralph-loop "修正终端中翻页模式的代码，直到在chrome浏览器中按PageUp/PageDown能正确对历史内容翻页"
 --max-iterations 15

/ralph-loop:ralph-loop "终端下方的方向输入面板打开时，点击面板之外区域，面板自动关闭，在chrome浏览器中测试"

当前任务IDLE判断的时长是多少。
判断逻辑：监控服务每10秒检查一次（POLL_INTERVAL = 10），如果终端内容连续45秒没有变化，则将任务状态标记为 IDLE。

在项目的Tasks列表页面，In Progress列表的上方增加TODO列表，

添加任务完成提醒。

* 增加常见图片格式的预览。

* 为编辑器中的文件增加函数跳转功能，鼠标放到函数或变量上时能看到定义。

* 隐藏tmux本身在终端显示的菜单。

* 增加新功能页：项目级git管理页面,可对项目所在分支进行管理。
对仓库进行pull操作，解决文件冲突。
对最近的commits进行push。可对每个commits的更改进行review。
在projects页面中每个project项右上角增加icon按钮入口。
git管理页面中，上方是header栏，显示项目名称及所属的分支，下方左侧是git操作区域，右侧是change预览区域。
适配移动端。

-----------------------------------------------
history:

在“Commits”增加右键菜单，可以对commit进行回退。

终端增加input按钮，点击时弹出多行输入框，先在网页上输入，再发送到终端，以减小延时。

diff view点击右上角的 上一个、下一个按钮，跳转到修改点时，不需要选中修改的代码。

Files列表中选中项的背景颜色与主题不符

SYMBOL preview,删除注释与signature的显示。在代码片段中，如果是原型，则显示该符号前后内容（与原来一致），
如果是函数或方法，则向前读取注释内容，向后读取整个函数体，显示函数的完整信息。

All locations放在preview内容最前面，删除原来代码片段上方的文件路径      
与行号。当all locations的项多于3行时，使用折叠（点击展开）

SYMBOL PREVIEW中有些函数信息不完整，例如“mosquitto_new”这个函数，它返回的signature：
“(const char * id,bool clean_session,void * obj)”，
没有函数名，也没有返回值，只有参数列表。

SYMBOL PREVIEW加载逻辑换一下。
用户在编辑器中点击了一个函数或定义时，后端查找所有SYMBOL，对结果SYMBOL进行排序（将原型排到前面），
首先显示排第一个的SYMBOL及相关信息，Docs/函数原型/代码片段等，就显示这个SYMBOL所在位置的就可以了，不用去找函数定义什么的。
如果这个SYMBOL是函数，代码片段就要显示出完整的函数代码，包含函数名本身。
点击最下方ALL LOCATIONS中的一项时，里边的相关信息要更新到对应SYMBOL。

我想增加符号预览功能：点击一个变量或函数，能在某个视图上查看到它的
Definition + JSDoc/docstring comments + type signature     
 and file path and line number (clickable to navigate)。
使用universal-ctags这个工具实现。

需要特殊处理：优先显示c/c++头文件中的doc与函数签名，如果该symbol是      
个函数，则需要显示完整的函数定义源码，其它类型的SYMBOL不需显示代码     
片断。同时显示SYMBOL相关的所有文件与行号的列表到SYMBOL                  
PREVIEW视图的最后。


编辑器中内容，不需要自动换行。

find_symbol中Prefer C/C++ header files for docstring/signature的优先级放在Prefer matches in context file之前

CodeReview界面右上角不需要退出登录的按钮，改为一个”设置“图标按钮，点击时弹出模态框。
增加对外部路径的index索引功能，用户在输入路径后（可多个路径），点右侧的index按钮，就可以将这些中的SYMBOL加入到数据库。
默认填写了”/usr/include/“这个目录，多目录使用";"分割。

移除Settings中的index功能，这里只用于设置额外的index目录列表，列表保存到数据库，这些数据只与对应的task相关联，不是全局的。
用户点击SYMBOAL PREVIEW右侧的index按钮时，除了worktree本身所在目录需要索引，这些用户额外增加的目录也需要。

index按钮应放在symbol preview的header上；前端界面，preview的header背景色应与SYMBOLS的header保持一致

CodeReview界面，不需要Running、Pending review这两个子标题状态，并减小整个header的高度；

我发现鼠标向一个函数按下时，立即触发了SYMBOL PREVIEW，我希望鼠标松开时才触发。
代码编辑器增加函数、变量列表功能（大纲outline），点击时编辑器能快速跳转到该符号。列表放在编辑器下方，以瀑布流的形式显示。

终端右上角当前文字是这样的：Connected e37e9f9c-9458-4d10-9d19-0ab7c8972ad3，不需要后面这一长串字符。

为节省流量，添加文件本地缓存功能。当打开一个文件时，先获取文件修改日期，与本地缓存文件比较，不一致时才需要拉取，否则直接加载本地的文件。

当屏幕宽度很小时，编辑器、diif视图等右上角的按钮要优先显示，文件名可以缩略。


Command Presets列表中的命令，右侧的删除图标按钮需要一直显示，
移除PDF文件预览相关功能，用户点击PDF文件时，在右侧编辑区域显示“使用浏览器打开该文件”，点击后浏览器会用自带的pdf阅读功能显示文件。

当前只有白天与黑夜两个主题，需要增加豆沙绿护眼、仿羊皮纸两种主题色。

我现在希望在终端中增加翻页模式（用于查看历史记录）。
当鼠标滚轮向上滚动或按page up按键时启用翻页模式（tmux启动复制模式），终端显示标题“Scroll Mode”。
按ESC后退出上述模式。

Quick Input面板，增加组件按钮shift +Tab。

终端下方控制栏，增加一个方向控制按钮"->"，点击后弹出方向按钮面板，将原来的几个方向按钮集成到面板中。

终端下方的按钮减小尺寸。

中文在“Changed files”列表中显示不正常，如“软件设计文档_LiHub_V1.3.md”显示成"\350\275\257\344\273\266\350\256\276\350\256\241\346\226\207\346\241\243_LiHub_V1.3.md"。
同时检查中文文件名在其它地方的显示是否有同样问题，特别是“Commits”列表中。

"Files"列表，增加文件上传与下载功能，做进右键菜单中。

Tasks列表页面刷新时避免抖动，并且增加刷新时的状态指示。减小刷新间隔到5秒。

Files列表弹出右键菜单后，点击列表中其它项，菜单不会隐藏，右键其它文件，菜单也不会跟随。

task中，将 commits merge回主分支过程中失败时，需要在原有内容上写清楚执行的命令。

增加文件快速跳转功能：按ctrl + p 时，弹出一个全局带遮罩的输入框，输入字符串，显示匹配此名字的文件列表，点击文件时，在编辑器中打开此文件。

增加markdown预览功能。如果该文件是markdown文件，则在编辑器右上角显示一个眼睛的图标，点击后全屏打开预览视图。

增加pdf类型文件预览功能。
移动端编辑器不需要显示行号与右侧滑动条的预览图。

accept\merge\save按钮，使用图标的形式，使风格与header栏的图标样式一致。

移动端header栏的"Files"按钮风格改为与其它按钮相同。

deploy.sh脚本里的中文都改成英文。

c语言的头文件与函数不能高亮显示。


获取"Commits"列表时，一次性返回所有文件的列表，占用资源过大，我希望通过翻页的方式，每次返回30条。

从worktree向主分支合并时，测试出现冲突的场景。

项目有些对话框使用了浏览器自带的，改为定制的。

Changed Files列表中增加右键菜单，与“Files”列表中的菜单做法一致。菜单项有copy path与revert。

移动端打开Commits中的一项显示时，不显示行号


将终端下方的View按钮名称改为"TEXT",其它按钮文字改成全大写。

Message Queue中的命令执行时，最后发送的回车符与命令间没看到间隔，延时无效。

移动端diff视图下，不需要显示行号，以增大显示区域。

终端下方的"Go"的按钮换成"Abc"按钮，点击后弹出对话框，上边有 A B C D E五个字母按钮，"Go"按钮，"Ctrl + C"按钮。点击后发送对应的字符，及延迟后的回车符，并关闭对话框。

“Files”、“Changed Files”、“Commits”三个列表的刷新机制需要增加自动触发刷新的方式。
例如用户保存、删除一个文件，成功后需要立即刷新“Changed Files”列表。
用户"Accept"了一次commit成功后，则需要立即刷新“Commits”列表。
在“Files”列表右上角增加一个手动刷新的图标按钮，允许用户立即刷新目录文件。


点击TODO按钮弹出的Message Queue对话框，不需要"Clear"按钮，转而增加一个默认勾选的勾选框"auto"，若该项被勾选时，自动在用户的命令后边增加".Don't ask me"。

Clear Queue按钮放到Queue列表下边，新增的auto勾选框与“Add同一行”，左对齐

当前任务处于Running或Idle状态是通过判断终端输出内容的hash，当终端尺寸变化时，hash值会因为增加了换行符而变化，而实际内容是不变的，请对这个情况进行处理。

编辑器中，如果按了ctrl + s 按钮，则调用保存文件功能。

创建项目时，Git Repository Path默认显示用户的home目录，用户输入时增加下拉形式的子目录列表，以便于用户选择，若用户选择了的目录不存在git repository ,则需要提示用户是否创建。

如果在“Changed Files”列表中点击了一个处于删除状态的文件，目前报错“400 (Bad Request)”，而我希望是在编辑器区域提示文件已删除。

“Changed Files”列表中的项，在右侧使用字符标记出该文件的状态，如文件是新增的，则标记为"+"，若删除则“D”，若修改则“M”等。

"Files"列表中的文件或目录项，增加右键菜单（移动端是长按），菜单中包含"delete"、"copy path"(复制相对路径) 等功能

"Commits"列表不能正常列出，始终为空。

"Files"列表未能显示所有文件，例如项目根目录下的“.env.example”就不能正常显示

* Changed Files列表未能显示所有未被git追踪的文件。

如果创建项目时，填写的并非是“main”分支，例如是"dev"分支，task还能正常合并代码到dev分支吗？请测试并确认此问题。如果项目所在源码被切换到其它分支，task中的commits合并时，是否自动切回dev分支并合并？


终端下方的控制栏增加一个"CMD"按钮，用于弹出显示预先设置的常用命令列表，用户可以在此添加删除命令，点击命令的的处理方式与之前的"Go"按钮相同。并增加一条预设置命令"claude --dangerously-skip-permissions"

终端下方的控制栏增加一个"TODO"按钮，用于弹出显示TODO队列列表功能，用户输入消息后，先放入队列（队列支持删除消息）。当检测到终端空闲时（逻辑已实现），取TODO队列中消息的发送到终端执行，每次一个，下一次空闲，又取一条执行。

在白天模式下，从task返回到task列表时，又被自动切到黑夜模式了。

这个panel显示与隐藏时不需要动画。

task页面需要移动端适配，原来已适配了，但失效了。


task页的Commits列表,隐藏git提交的标题，将现有的时间字符串作为标题，时间串格式化为本地时间字符串。

终端下方的按钮不能正常显示，超出区域之外了。

桌面模式下，task页面的“Changed Files”、"Files"、“Commits”需要能拖动调整区域大小
、
task页面中，"Merge"前，弹出对话框，让用户填写具体的的变更内容，内容默认为task名称


task列表中的每个task，需要在右侧增加一个删除任务的图标按钮，点击后弹出删除确认对话框，可将任务删除（需同时删除对应的worktree）

last_merge_commit_hash应保存work tree最后merge时的最新commit hash，而不是merge成功后主分支的hash。

task页面中，每次Merge前，需要将所对应的主分支代码合并到当前worktree分支，如果合并过来的代码有冲突，停止继续merge并提示用户先解决冲突。没有发现冲突后，才继续将当前worktree分支的commits合并回 主分支，若合并回主分支发现merge失败，应将主分支的这次merge撤回。

http://localhost:5173/tasks/0c6cbaf9-cc52-4088-8267-c0d62172d8d2页面中的“Merge”按钮一直是灰色的不可点击状态，找出原因
removing the redundant task?.status === 'merged' check from both Merge buttons in CodeReviewView.vue.

在Merge按钮左侧增加一个"Pull"按钮，将origin分支上其它worktree merge上去的commit同步下来。同步下来后，更新数据库中的last merge hash。

commits列表中，一直没有出现指示last merge所在的勾勾指示图标

merge按钮移动到"Commits"列表标题同一行，右对齐

任务的“待合并”状态，改为通过last_merge_commit_hash判断（last_merge_commit之后如果还有新的commit，则是待合并状态），task页的Commits列表中，将最后一个merge的时间点的item加个标记（hash文字前面加个绿色的指示）

task页面手动刷新时，界面显示空白。

“Changed Files”、“Files”、“Commits”点击其中的项时，均在右侧编辑区域切换到对应的编辑器或diff比较视图。

task页，在“Files”下方，增加一个新的"Commits"列表，用于显示此task对应的worktree下有哪些未合并的提交。

task页的Commits列表点击时，能够显示文件变更内容。

为子任务增加两种状态显示。
1、任务状态（终端中程序运行的状态）：执行中、空闲。后端持续监控tmux会话里的程序是否停止打印内容，通过监控 tmux 窗格的输出缓冲区变化，对比最新100行的内容哈希（tmux的capture-pane命令）；
2、代码状态：待审查（worktree中有代码变更，这些代码没有accept）、待合并（worktree中有已提交(accepted)的代码，但代码没有合并到主分支），无更改（既无代码更改，也没有需要合并的commit）。
项目下的子任务列表页需分类，大类分为进行中与已完成（上述状态中任务空闲、代码无更改，则认为是“已完成”的），子任务条目的右下角显示上边两个的状态。
task页中的Merge按钮与Accept按钮增加disable状态，只在相应状态才enable。

创建项目时，增加可勾选的“自动创建git仓库”

task页面中增加“Accept”按钮，以同意当前变更并提交。原merge功能改为将当前worktree已提交的变更合并到项目所在分支

创建项目时，所需填写的分支名称自动获取当前git分支名。

点击终端下方的“View”按钮时，在终端上方显示一个只读取的编辑器（也使用monaco-editor），显示终端的历史输出内容（历史缓存），缓存使用tmux自带的缓冲，tmux启动会话同时设置缓冲行数为50000, 命令：tmux new-session -s mysession -c ~ 'tmux set -g history-limit 50000;'。使用 tmux capture-pane 命令指定目标会话导出历史内容到临时文件，之后加载内容到前述的编辑器中，加载完毕后删除临时文件。

终端下方增加控制菜单栏，有上下左右四个方向按键、“Yes”、“Start”、“View”等按钮,方向按键效果与键盘的方向键效果一样，“Yes”、“Start”时，发送这些关键字到命令行并回车，“View”按钮功能待定，颜色与终端颜色相近即可，不需要跟着主题变换。


某个task打开终端时，如果没有找到已有的tmux会话（报no sessions），则需要重新创建一个并连接它。

当打开的是diff比较页时，在save按钮左侧增加新按钮“Next Diff”，点击时滑动到下一个变化的位置

task页，当在移动端使用时，顶部新增按钮“File list”，用于弹出显示或隐藏“Changed Files”和"Files”控件（它们显示在编辑器上方），移动端terminal显示时，覆盖整个编辑区域，而不是与编辑器并排。


“Changed Files”刷新时文字不要抖动，不要显示指示圈圈，获取到结果时刷新即可。


阅读term.js的官方API文档（https://xtermjs.org/docs/），重构terminal的前后端实现，实现一个完整的、支持TUI文本界面程序运行的终端。

diff view不能正确展示变更的代码行。

Changed Files刷新间隔改为5秒。

Changed Files与files这两个控件高度，保持1比2的比例。
